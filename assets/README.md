Place the hockey player sprite at `assets/hockey_player.png`.

Recommended:
- Use the provided PNG (player on transparent background).
- Aim for an image height around 56–128px; the code will scale it to ~56px high by default.

If you name the file differently, update `hockeySprite.src` in `script.js` accordingly.
